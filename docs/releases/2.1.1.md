# 2.1.1

## Blob/lob drag improvements

- When I drag the capability onto a domain, the domain shape doesn't automatically extend the lobe to "receive" that capability; this only works if I drag the capability out and back into the same domain (not others) without releasing the mouse button.

## Line width improvements

- The lines are too thin when I zoom out; keep the line width proportional at any zoom level.
- It is difficult to select a line on a phone screen with tap -- make the tap aria wider.

## Version rename

- Return the possibility to name map versions.